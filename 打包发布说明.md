# 学生公寓管理系统双模式说明

现在这个项目已经支持两种运行模式：

- `MySQL 模式`：保留你原来的课程设计形式，用来展示存储过程、触发器、事务等数据库设计内容。
- `SQLite 模式`：新增的便携演示版，用来打包成软件发给别人，别人不需要安装 MySQL。

## 一、三个启动入口

### 0. 图形选择器

使用：

```powershell
.\.venv\Scripts\python.exe launcher.py
```

或者直接双击：

```text
启动系统选择器.bat
```

特点：

- 打开一个图形界面
- 可以在 `MySQL 课程设计版` 和 `SQLite 便携演示版` 之间选择
- 更适合平时自己演示使用

### 1. MySQL 课程设计版

使用：

```powershell
.\.venv\Scripts\python.exe run_mysql.py
```

或者双击：

```text
启动MySQL版.bat
```

特点：

- 继续连接 [config.py](/e:/pycharm%20project/dblab2/config.py:1) 里的 MySQL
- 保留你原来的课程设计结构
- 适合答辩、展示数据库设计

### 2. SQLite 便携版

使用：

```powershell
.\.venv\Scripts\python.exe run_sqlite.py
```

或者双击：

```text
启动SQLite版.bat
```

特点：

- 首次启动会自动创建 `student_apartment_system.db`
- 自动写入一套演示数据
- 不需要安装 MySQL
- 更适合发给同学或老师直接运行

## 二、新增改动

### 已新增

- [database.py](/e:/pycharm%20project/dblab2/database.py:1)
- [run_mysql.py](/e:/pycharm%20project/dblab2/run_mysql.py:1)
- [run_sqlite.py](/e:/pycharm%20project/dblab2/run_sqlite.py:1)

### 已调整

- [app.py](/e:/pycharm%20project/dblab2/app.py:1)
- [config.py](/e:/pycharm%20project/dblab2/config.py:1)
- [build_exe.bat](/e:/pycharm%20project/dblab2/build_exe.bat:1)

## 三、SQLite 模式是怎么兼容现有代码的

为了尽量不破坏原来的课程设计代码，没有把所有页面逻辑重写，而是做了一个数据库适配层：

- MySQL 模式下，仍然走 `PyMySQL`
- SQLite 模式下，自动把 `%s` 占位符转换成 SQLite 可用格式
- 自动兼容 `NOW()`、`CURDATE()` 这类常见 MySQL 写法
- 对 `CALL proc_auto_assign_room(...)` 做了 SQLite 下的等价实现

这样做的结果是：

- 原来的大部分业务代码可以继续保留
- SQLite 版也能正常登录、查询、分配宿舍、发布公告、提交报修等

## 四、怎么打包给别人

现在 [build_exe.bat](/e:/pycharm%20project/dblab2/build_exe.bat:1) 默认打包的是 `SQLite 对外发布版`。

执行：

```powershell
.\build_exe.bat
```

生成目录：

```text
dist\StudentApartmentSystemSQLite
```

对外发送时，请发送整个目录，不要只发单个 `exe`。

对方运行：

```text
StudentApartmentSystemSQLite.exe
```

首次运行时会自动创建 SQLite 数据库文件，不需要安装 MySQL，也不会看到 MySQL 相关选项。

## 五、一键生成可发送压缩包

如果想直接生成一个适合发送的压缩包，可以执行：

```powershell
.\build_release_zip.bat
```

生成文件：

```text
release\StudentApartmentSystemSQLite.zip
```

这个压缩包更适合直接发给老师、同学或答辩展示时拷贝到别的电脑。


原因是当前采用的是 `PyInstaller onedir` 打包方式，程序运行时还需要同目录下的 `_internal` 资源文件夹，其中包含 Tkinter 依赖的 `init.tcl` 等文件。

